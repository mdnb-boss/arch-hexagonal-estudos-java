server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hexagonal_arch_db?createDatabaseIfNotExist=true&useTimezone=false&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect